<?xml version="1.0" encoding="UTF-8"?>
<!--
  Without a configuration Logback defaults to DEBUG for everything, which makes Netty log its
  entire platform probe at startup and puts logging on the ingest path. Tests keep it at WARN so
  failures are still visible and measurements are not distorted by the harness.
-->
<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} -- %msg%n</pattern>
    </encoder>
  </appender>

  <logger name="io.netty" level="WARN"/>
  <logger name="org.apache.kafka" level="WARN"/>
  <logger name="org.testcontainers" level="WARN"/>
  <logger name="com.github.dockerjava" level="WARN"/>
  <logger name="io.github.mustaffadnc.suru" level="INFO"/>

  <root level="WARN">
    <appender-ref ref="CONSOLE"/>
  </root>
</configuration>
